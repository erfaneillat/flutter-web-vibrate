name: flutter_web_vibrate
description: "Flutter web plugin for the Vibration"
version: 0.0.1
homepage: https://github.com/erfaneillat/flutter-web-vibrate

environment:
  sdk: ^3.5.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  js: ^0.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
